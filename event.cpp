#include"dork.h"
/*
int checkEvent(int x, int y)
{
	Database(eventLocationX,eventLocationY);
	string answer;
	for(int i = 1; i<200; i++)
	{
	   if((x == eventLocationX[i]) && (y == eventLocationY[i]))
	   {
		if(response[i] == 1)
		{
			printw("%s",eventDescription[i].c_str());
			scanw("%s",answer.c_str());
		}
		else
		{
			if(energyNum[i] != 0)
			{
				return energyNum[i];
			}
			if(moveX[i] != 0)
			{
				return moveX[i];
			}
			if(moveY[i] != 0 )
			{
				return moveY[i]:
			}
		}	
	   }
	}
}*/
